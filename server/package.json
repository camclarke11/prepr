{
  "name": "prepr-api",
  "private": true,
  "type": "module",
  "description": "prepr real-time sync + push notifications API (Cloudflare Worker + Durable Object).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@pushforge/builder": "^2.0.5"
  }
}
